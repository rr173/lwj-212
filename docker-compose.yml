services:
  workbench:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - db_data:/data
    environment:
      - DB_PATH=/data/protocol_workbench.db
    restart: unless-stopped

volumes:
  db_data:
